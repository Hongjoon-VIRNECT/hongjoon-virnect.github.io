function myfunction(){
    if ('Accelerometer' in window) {
        // The `Accelerometer` interface is supported by the browser.
        // Does the device have an accelerometer, though?
        document.write("Yes, it is.")
    }
    document.write("This is button test");
}
